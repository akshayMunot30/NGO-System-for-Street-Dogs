import 'package:flutter/material.dart';

class WeelkyReports extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Weekly reports'),
      ),
    );
  }
}
